# Task Orchestrator

A Rest API that manages docker containers.

## Setup

```bash
$ virtualenv venv
$ source ./venv/bin/activate
$ pip install -r requirements.txt
```

## Run the app

```bash
$ ./serve.sh
```
